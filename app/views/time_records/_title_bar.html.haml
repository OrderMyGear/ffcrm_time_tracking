#confirm{ hidden }
.title_tools#menu
  = link_to_function(t(:delete) + '?', confirm_delete(@time_record))
.title_tools#buttons
  = view_buttons
.title#edit_time_record_title
  = h(t(:time_record))
  = image_tag("loading.gif", :size => :thumb, :id => "loading", :style => "display: none;")

%div#edit_time_record{ hidden }
